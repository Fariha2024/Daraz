// components/Header.tsx

import React from 'react';
import NavBar from './navigation'; // Import the Navbar

const Header: React.FC = () => {
  return (
    <header>
      <NavBar /> 
      </header>
  );
};

export default Header;
